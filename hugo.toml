[markup]
	[markup.goldmark]
		renderer.unsafe = true

[module]
    [[module.mounts]]
        source = "assets/icons"
        target = "static/icons"

    [[module.mounts]]
        source = "assets/img"
        target = "static/img"